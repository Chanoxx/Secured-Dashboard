<!DOCTYPE html>
<html>
  <head>
    <meta charset="UTF-8">

    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.0/css/all.min.css">

    <title>{% block title %}Trailblaze{% endblock %}</title>

    {% block stylesheets %}
      <link href="https://fonts.googleapis.com/css2?family=Montserrat:wght@400;700;900&family=Roboto:wght@400;500&display=swap" rel="stylesheet">
      <link rel="stylesheet" href="{{ asset('css/trailblaze.css') }}"> 
    {% endblock %}
  </head>

  <body>

    <nav class="user-nav">
    <a href="{{ path('user_profile') }}">Profile</a>
        <a href="{{ path('shop') }}">Shop</a>
{% if app.user %}
    <a href="{{ path('orders_list') }}">My Orders</a>

{% endif %}
        {% if app.user %}
            <a href="{{ path('cart') }}" class="cart-icon">
                My Cart  
            </a>
            <a href="{{ path('app_logout') }}">Logout</a>
        {% else %}
            <a href="{{ path('app_login') }}">Login</a>
        {% endif %}

        {% if app.user %}

{% endif %}

    </nav>

    {% block body %}{% endblock %}

  </body>
</html>
